'use strict';

/**
 * option-traitement service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::option-traitement.option-traitement');
