'use strict';

/**
 * dimension-document service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dimension-document.dimension-document');
