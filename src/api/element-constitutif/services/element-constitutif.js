'use strict';

/**
 * element-constitutif service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::element-constitutif.element-constitutif');
