'use strict';

/**
 * localisation-adresse service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::localisation-adresse.localisation-adresse');
